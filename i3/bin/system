#!/bin/bash

SELECTED=$(echo -e "Shutdown\nLock\nReboot\nLogout\nExit" | dmenu -i)

case $SELECTED in
    "Shutdown") shutdown -h now ;;
    "Lock") i3lock --color "#2f343f" ;;
    "Reboot") shutdown -r now ;;
    "Logout") i3-msg exit ;;
    *) ;;
esac